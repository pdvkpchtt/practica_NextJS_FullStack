const SunscriptionsLayout = ({ children }) => {
  return (
    <div className="[@media(pointer:coarse)]:mt-[38px] [@media(pointer:coarse)]:overflow-hidden [@media(hover)]:mt-[62px] w-full">
      {children}
    </div>
  );
};

export default SunscriptionsLayout;
